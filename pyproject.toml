[project]
name = "proekt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ina", email = "ina.bgina1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
proekt = "proekt:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
